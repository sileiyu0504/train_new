# RGB-D training/validation using pseudo-depth generated by Depth Anything
path: /mnt/e/Desktop/train_new/datasets/YOLO_RGB_carton
train: images/train2017
val: images/val2017

depth:
  train: depth/train2017
  val: depth/val2017
depth_scale: 65535.0
# Per-image min-max normalization is enabled in YOLODataset (depth_per_image_norm=True)

names:
  0: carton
